"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { createClient } from "../../../lib/supabase/client";
type Payment={id:string;mission_id:string;hours:number;candidate_hourly_rate:number;extra_amount:number;status:string;paid_at:string|null;created_at:string};
export default function Paiements(){
 const [items,setItems]=useState<Payment[]>([]);const [message,setMessage]=useState("Chargement…");
 useEffect(()=>{(async()=>{const s=createClient();const {data:{user}}=await s.auth.getUser();if(!user){location.href="/connexion";return;}const {data,error}=await s.from("payment_records").select("id,mission_id,hours,candidate_hourly_rate,extra_amount,status,paid_at,created_at").eq("extra_id",user.id).order("created_at",{ascending:false});if(error)setMessage("Aucun paiement disponible pour le moment.");else{setItems(data||[]);setMessage("");}})()},[]);
 return <main className="appShell"><header className="appHeader"><Link className="brand" href="/">FOOD<span>FORCE</span></Link><div className="appName">EXTRAS</div><nav><Link href="/extras">Missions</Link><Link href="/extras/mes-missions">Mes missions</Link><Link href="/extras/profil">Profil</Link></nav></header><section className="pageIntro"><span className="eyebrow">FOODFORCE EXTRAS</span><h1>Mes paiements.</h1><p>Retrouvez les montants générés après validation de vos heures.</p></section><section className="missionGrid">{items.map(p=><article className="missionCard" key={p.id}><span className={p.status==="paid"?"tag acceptedTag":"tag"}>{p.status}</span><h3>{Number(p.extra_amount).toFixed(2)} MAD</h3><p>{p.hours} h × {Number(p.candidate_hourly_rate).toFixed(2)} MAD/h</p><small>{p.paid_at?"Payé le "+new Date(p.paid_at).toLocaleDateString("fr-FR"):"En attente de paiement"}</small></article>)}</section>{message&&<p className="statusMessage">{message}</p>}</main>